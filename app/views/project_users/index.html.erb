<h1>ProjectUsers#index</h1>


<table id="project_users" border=2 class="ui celled table">
	<thead>
		<th>project_id</th>
		<th>user_id</th>
		<th>role_id</th>
		<th></th>
		<th></th>
		<th></th>
	</thead>
	<tbody>
		<% @project_users.each do |p| %>
			<tr>
			<td><%=p[0]%></td>
			<td><%=p[1]%></td>
			<td><%=p[2]%></td>
			<td><%= link_to "view",  project_user_path(p[3]) %></td>
			<td><%= link_to "edit", edit_project_user_path(p[3]) %></td>
			<td><%=link_to 'delete', project_user_path(p[3]) , method: :delete, data: { confirm: 'record will be deleted.Are you sure?' }%></td>
			</tr>
		<%end%>
	</tbody>
</table>


<%= link_to "new record", new_project_user_path %>

<script>
	$(document).ready(function(){
		 $("#project_users").dataTable()
	});
</script>